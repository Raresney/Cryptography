{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd app && npm install && npm run build"
  },
  "deploy": {
    "startCommand": "npx serve app/dist -s -l $PORT",
    "restartPolicyType": "ON_FAILURE"
  }
}
